# Data

Place the NASA C-MAPSS raw dataset files here when running the notebooks locally.

Raw dataset files are intentionally not committed to this repository. Keep files such as `train_*.txt`, `test_*.txt`, and `RUL_*.txt` local.
